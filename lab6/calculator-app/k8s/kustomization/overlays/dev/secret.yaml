apiVersion: v1
kind: Secret
metadata:
  name: calculator-secret
  labels:
    app: calculator
type: Opaque
stringData:
  # API_KEY из lab5
  API_KEY: top-secret-key
  # DATABASE_URL согласован с контрактом telegram-support-infra (тот же namespace calculator-demo)
  DATABASE_URL: "postgres://postgres:postgres@postgres-0.postgres:5432/support_bot?sslmode=disable"
