apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: calculator-prod

resources:
  - ../../base
  - secret.yaml

patches:
  - path: patch-deployment.yaml

commonLabels:
  environment: prod
