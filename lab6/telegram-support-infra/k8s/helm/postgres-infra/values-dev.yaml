# Dev: малый том, слабые ресурсы, простой пароль (только для локального стенда).

password: postgres

persistence:
  size: 1Gi

resources:
  requests:
    memory: 128Mi
    cpu: 50m
  limits:
    memory: 384Mi
    cpu: 500m

podLabels:
  environment: dev
